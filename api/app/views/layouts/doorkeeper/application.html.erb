<!DOCTYPE html>
<html>
<head>
  <title><%= t('doorkeeper.layouts.application.title') %></title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <% unless Rails.env.test? %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", defer: true %>
  <% end %>

  <%= csrf_meta_tags %>
</head>
<body class="bg-neutral-50 dark:bg-neutral-900 min-h-screen flex items-center justify-center">

  <div class="flex flex-col items-center justify-center w-full max-w-sm gap-4 p-8">

    <%- if flash[:notice].present? %>
      <div class="w-full text-center rounded-md bg-blue-50 px-4 py-2 text-sm text-blue-500 dark:bg-blue-900/20 dark:text-blue-200">
        <%= flash[:notice] %>
      </div>
    <% end -%>

    <%= yield %>
    
  </div>

</body>
</html>
