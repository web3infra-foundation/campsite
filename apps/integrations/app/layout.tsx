// Next.js automatically generates this file if it doesn't exist
export const metadata = {
  title: 'Integrations',
  description: 'Integrations'
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang='en'>
      <body>{children}</body>
    </html>
  )
}
